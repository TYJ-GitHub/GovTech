# GovTech
Ensure nodejs is downloaded.

Run terminal:
npm install express
npm install body-parser
npm install node-fetch
npm install cors
npm install axios
npm install cheerio
npm install jsdom
npm install path
